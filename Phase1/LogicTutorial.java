class LogicTutorial{
    public static void main(String[] args) {
        /*
            AND     && 
            OR      ||
            NOT     !   (opposit)
        */

        int a = 20,b = 20;

        boolean c = (a == b); // true
        boolean d = (a < b); // true 

        System.out.println((a == b) && (a <= n)); //===> (true && true)
        System.out.println((a == b) || (a < n)); //===> (true || false)
        System.out.println(!(a == b)); // true ==> false
    }
}